import React, { useEffect, useRef, useState } from 'react';
import gsap from 'gsap';

function CustomCursor() {
  // Disable custom cursor → restore normal system cursor
  return null;
}


export default CustomCursor;
